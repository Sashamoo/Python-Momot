#include <iostream>
using namespace std;

int main() {
    /* Завдання 1 */
   
    /*
    {
        int width, height;
        char ch;

        cout << "Введіть ширину: ";
        cin >> width;
        cout << "Введіть висоту: ";
        cin >> height;
        cout << "Введіть символ: ";
        cin >> ch;

        for (int i = 0; i < height; i++) {
            for (int j = 0; j < width; j++) {
                cout << ch;
            }
            cout << endl;
        }
    }

    cout << endl; // розділення між завданнями
    */

    /* Завдання 2 */
    
    /*
    {
        int width, height;
        char ch;

        cout << "Введіть ширину: ";
        cin >> width;
        cout << "Введіть висоту: ";
        cin >> height;
        cout << "Введіть символ: ";
        cin >> ch;

        for (int i = 0; i < height; i++) {
            for (int j = 0; j < width; j++) {
                if (i == 0 || i == height - 1 || j == 0 || j == width - 1) {
                    cout << ch;
                } else {
                    cout << " ";
                }
            }
            cout << endl;
        }
    }
    */

    return 0;
}

